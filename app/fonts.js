import { Inter, Kaisei_Opti } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const kaisei_opti = Kaisei_Opti({
  subsets: ["latin"],
  display: "swap",
  weight: ["400", "500", "700"],
});
